<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Theme for the splash screen with no actionbar-->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="LoginTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--EditText hint color-->
        <item name="android:textColorHint">@color/color_default_grey</item>
        <!-- TextInputLayout text color-->
        <item name="colorControlActivated">@color/color_default_black</item>
        <!-- EditText line color when EditText on-focus-->
        <item name="colorControlHighlight">@color/colorPrimary</item>
        <!-- EditText line color when EditText  in un-focus-->
        <item name="colorControlNormal">@color/color_default_grey</item>
    </style>

    <!--customized error text color for text input layout-->
    <style name="error_appearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/color_default_red</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!--customized style for floating action button-->
    <style name="FabStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">@dimen/fab_margin</item>
        <item name="borderWidth">0dp</item>
        <item name="elevation">6dp</item>
        <item name="pressedTranslationZ">12dp</item>
        <item name="rippleColor">@android:color/white</item>
    </style>


</resources>
